[project]
name = "model-error-translator"
version = "0.1.0"
description = "Refuses a bad model request before it is sent, and turns a raw model failure into one typed code and one artist-readable sentence."
requires-python = ">=3.10"
license = { text = "Evaluation-Only Licence 1.0 (source-available, not open source); see LICENSE" }
authors = [{ name = "Vo Ba Hoang Minh" }]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
